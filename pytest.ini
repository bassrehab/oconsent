# pytest.ini

[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests
    integration: Integration tests
addopts = -v --cov=oconsent --cov-report=html

# Exclude integration tests by default
# Run with: pytest -m "integration" to include them